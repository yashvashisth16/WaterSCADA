# WaterSCADA

Industrial SCADA system built with Python.

## Structure

- `industrial_project.py` — main application entry point
- `core/` — core modules for alarms, database, GUI, PLC communication, trends, and hardware
- `requirements.txt` — project dependencies

## Notes

This repo excludes generated builds, the virtual environment, and runtime database files.
